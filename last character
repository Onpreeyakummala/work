#include<stdio.h>
#include<string.h>
int main(){
	char str[50];
	char *ptr;
	ptr = str;
	printf("Input: ");
	scanf("%s", str);	
	while(*ptr != '\0')	ptr++;
	ptr--;
	printf("last character = %s\n" , ptr);
	return 0;
}
